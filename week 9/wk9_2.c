#include <stdio.h>

// 65010332 ณิชกานต์ ใจติ๊บ
// รับค่าตัวเลข 2 จำนวน เก็บใน x และ y ตามลำดับ จากนั้นให้ทำการสลับค่ากัน โดยห้ามสร้างตัวแปรเพิ่ม
int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    a = a^b;
    b = a^b;
    a = a^b;
    printf("swab : %d %d", a, b);
    return 0;
}